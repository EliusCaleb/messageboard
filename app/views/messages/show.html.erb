<h2> <%= @message.title %></h2>
<p> <%= @message.discription %></p>
  <%= link_to "Back", root_path, class: "btn btn-default" %>
 <%= link_to "Edit", edit_message_path(@message), class: "btn btn-default" %>
 <%= link_to "Delete", message_path(@message), method: :delete, class: "btn btn-default", data: { confirm: "Are you sure you want to delete this message?" } %>